# yaml-language-server: $schema=../../../../yuruna/test/schemas/sequence.schema.yml
#
# AmisAd POC - s003.silence (Consent Revocation and the Right to Silence):
# restore amisad-core - that restore IS the per-scenario state reset.

sequenceGuid: 42b0e16a-99fa-4086-b2b2-6227ba35ea6a
sequenceRevision: 2

description: "AmisAd s003.silence: restore amisad-core, pause/withdraw/resume Maya's consent, assert silence, honored commitments, and the immutable consent history."
keystrokeMechanism: ssh
resource:
  ubuntu.server.24:
    - workload.guest.ubuntu.server.24.amisad-core.deploy
requiresSnapshot:
  id: "amisad-core"
variables:
  username: amisad-core-admin
  hostname: amisad-core

component:
  - action: loadDiskSnapshot
    id: "amisad-core"
    description: "Restore the deployed AmisAd vm-core (per-scenario state reset)"

  - action: sshWaitReady
    timeoutSeconds: 300
    description: "SSH up after the restore"

workload:
  - action: sshFetchAndExecute
    command: "/usr/local/lib/yuruna/fetch-and-execute.sh project/poc/test/ubuntu.server.24/ubuntu.server.24.amisad-core.s003.silence.sh"
    timeoutSeconds: 1800
    description: "Run s003.silence consent revocation flow (edge on amisad-edge-a), assert full TVP"

  - action: saveSystemDiagnostic
    id: "amisad.s003.silence"
    description: "Capture diagnostics after the s003.silence run"

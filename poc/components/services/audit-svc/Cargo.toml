[package]
name = "audit-svc"
version = "0.1.0"
edition = "2021"
description = "Independent verification of ledgers, attestation chains, residency, and consent (AmisAd POC skeleton)"

[dependencies]
amisad-common = { path = "../../lib/amisad-common" }

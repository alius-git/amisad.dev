[package]
name = "seller-svc"
version = "0.1.0"
edition = "2021"
description = "Seller catalog, inventory, order lifecycle, and settlement views (AmisAd POC skeleton)"

[dependencies]
amisad-common = { path = "../../lib/amisad-common" }
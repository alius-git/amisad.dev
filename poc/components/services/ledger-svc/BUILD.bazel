load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "ledger-svc",
    srcs = ["src/main.rs"],
    edition = "2021",
    deps = [
        "//components/lib/amisad-common",
        "@crates//:postgres",
    ],
    visibility = ["//visibility:public"],
)

exports_files(["Cargo.toml"])

[package]
name = "connect-svc"
version = "0.1.0"
edition = "2021"
description = "Supply-side integration gateway: contracts, sandbox tenants, workload credentials (AmisAd POC skeleton)"

[dependencies]
amisad-common = { path = "../../lib/amisad-common" }

# `amisad.end-to-end` is an orchestration sequence (InvokeTestSequence steps):
# The unattended runner detects the
# orchestration entry and delegates the whole cycle to it (one dashboard row per
# inner sequence)
sequences:
  - amisad.end-to-end
